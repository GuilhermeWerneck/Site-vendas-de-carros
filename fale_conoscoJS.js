
function validar(){ 
    var area = document.getElementById("area_explicar_problema").value;

    if (area == "" ) {
        alert("Por favor preencha o campo de explicação ")
    } else{ 
        alert("Reclamação enviada com sucesso")
    }
    
}